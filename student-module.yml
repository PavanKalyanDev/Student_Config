# GitHub లో ఫైల్ పేరు: student-module.yml

spring:
  application:
    name: student-module     # ← ఇక్కడ Student_Module కాదు → student-module

server:
  port: 8081

  # --------------------- DATABASE ---------------------
  datasource:
    url: jdbc:mysql://localhost:3306/studentsdata?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Pspk#@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  # --------------------- EMAIL ---------------------
  mail:
    host: smtp.gmail.com
    port: 587
    username: ppspk7263@gmail.com
    password: ljts mlxi xafi cofo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: true

  sql:
    init:
      mode: never
